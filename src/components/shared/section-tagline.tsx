export const SectionTagline = ({ children }: { children: React.ReactNode }) => {
  return (
    <p className="text-sm font-medium uppercase tracking-wider text-stone-600">
      {children}
    </p>
  );
};
